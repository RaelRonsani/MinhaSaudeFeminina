providers = ["php"]

[phases.setup]
nixPkgs = ["php82", "php82Extensions.curl", "php82Extensions.dom", "php82Extensions.mbstring"]

[phases.build]
cmds = ["echo 'PHP app - no build necessario'"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public"
